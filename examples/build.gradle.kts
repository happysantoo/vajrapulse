// Aggregator project for examples.
// Intentionally left minimal: no plugins applied to avoid creating
// unused configurations (e.g., annotationProcessor) that could trigger
// unsafe resolution warnings.

group = "com.vajra"
version = "1.0.0-SNAPSHOT"

// If needed in future, we can declare common example-wide configuration here.
